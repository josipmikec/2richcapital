<?php
define('WP_USE_THEMES', false); require_once dirname(__DIR__, 3) . '/wp-load.php'; if(!current_user_can('manage_options'))wp_die('Forbidden',403); global $wpdb; require_once ABSPATH.'wp-admin/includes/upgrade.php'; $c=$wpdb->get_charset_collate();$s=$wpdb->prefix.'rich_market_symbols';$b=$wpdb->prefix.'rich_market_candles';$y=$wpdb->prefix.'rich_market_sync_state';dbDelta("CREATE TABLE {$s} (id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT, provider VARCHAR(20) NOT NULL DEFAULT 'mt5', broker_account_id BIGINT UNSIGNED NULL, broker_name VARCHAR(120) NULL, broker_server VARCHAR(190) NULL, mt5_symbol VARCHAR(80) NOT NULL, display_symbol VARCHAR(40) NOT NULL, asset_class VARCHAR(20) NULL, digits TINYINT NULL, point_size DECIMAL(24,12) NULL, timezone VARCHAR(64) NULL, enabled TINYINT(1) NOT NULL DEFAULT 1, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id), UNIQUE KEY broker_symbol(provider,broker_account_id,mt5_symbol), KEY display_symbol(display_symbol)) {$c}");dbDelta("CREATE TABLE {$b} (id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT, symbol_id BIGINT UNSIGNED NOT NULL, timeframe VARCHAR(8) NOT NULL, candle_time_utc DATETIME NOT NULL, open_price DECIMAL(30,12) NOT NULL, high_price DECIMAL(30,12) NOT NULL, low_price DECIMAL(30,12) NOT NULL, close_price DECIMAL(30,12) NOT NULL, tick_volume BIGINT UNSIGNED NULL, real_volume BIGINT UNSIGNED NULL, spread_points INT NULL, is_closed TINYINT(1) NOT NULL DEFAULT 1, source VARCHAR(20) NOT NULL DEFAULT 'mt5', broker_name VARCHAR(120) NULL, broker_server VARCHAR(190) NULL, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id), UNIQUE KEY candle_unique(symbol_id,timeframe,candle_time_utc,source), KEY lookup(symbol_id,timeframe,candle_time_utc)) {$c}");dbDelta("CREATE TABLE {$y} (id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT, symbol_id BIGINT UNSIGNED NOT NULL, timeframe VARCHAR(8) NOT NULL, last_closed_candle_utc DATETIME NULL, last_attempt_at DATETIME NULL, last_success_at DATETIME NULL, last_error_code VARCHAR(64) NULL, last_error_message TEXT NULL, consecutive_failures INT NOT NULL DEFAULT 0, rows_synced BIGINT UNSIGNED NOT NULL DEFAULT 0, locked_until DATETIME NULL, PRIMARY KEY(id), UNIQUE KEY sync_unique(symbol_id,timeframe)) {$c}");wp_die('Market tables installed.');
